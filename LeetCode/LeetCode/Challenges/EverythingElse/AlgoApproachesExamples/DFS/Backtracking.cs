namespace LeetCode.Challenges.EverythingElse.AlgoApproachesExamples.DFS
{
    //Subsets, Combinations
    //Permutations
    //Sudoku Solver, N-Queens
    //Palindrome Partitioning, Word Break
    //Generate Parentheses
    //Maze Solver, Word Search

/* 
 * O(K^N), где N - размер входных данных, 
 * а K - количество ветвей, на которые разбивается задача на каждом шаге. 
 * В худшем случае, когда перебираются все возможные варианты, сложность может достигать факториальной, 
 * то есть O(N!) 
 */
public class Backtracking
{
    public Backtracking()
    {
//        void Backtrack(List<int> path, int start)
//        {
//            if (условие завершения)
//{
//                results.Add(new List<int>(path));
//                return;
//            }

//            for (int i = start; i < candidates.Length; i++)
//            {
//                if (someConstraint) continue;

//                path.Add(candidates[i]);
//                Backtrack(path, i + 1); // или i, если можно использовать элемент повторно
//                path.RemoveAt(path.Count - 1); // ← откат
//            }
//        }
    }
}
}
